{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm -w apps/api run build && ls -la apps/api/dist/"
  },
  "deploy": {
    "startCommand": "npm -w apps/api run prisma:migrate:deploy && npm -w apps/api run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

